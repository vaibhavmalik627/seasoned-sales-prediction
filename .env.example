PORT=5000
ML_SERVICE_URL=http://localhost:8000
CORS_ORIGIN=*

# This backend does not use MongoDB, Cloudinary, or JWT-based auth.
# Do not add placeholder secrets unless the application actually implements them.
